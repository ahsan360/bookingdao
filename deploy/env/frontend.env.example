# ─── Frontend Production Environment ─────────────────────────
# API URL - tenant subdomains proxy /api/ to backend via Nginx
NEXT_PUBLIC_API_URL=https://api.bookease.com/api

# App domain (for subdomain detection logic)
NEXT_PUBLIC_APP_DOMAIN=bookease.com
